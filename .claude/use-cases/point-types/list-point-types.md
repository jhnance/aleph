# List Point Types

An org member views the available point types and frontend frameworks when creating or editing a point. Returns platform-provided types/frameworks (`organization_id IS NULL`) and org-defined ones (`organization_id = current org`), ordered platform-first.
