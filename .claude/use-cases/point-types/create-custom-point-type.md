# Create Custom Point Type

An org member creates an org-defined custom point type. The new type is only visible to and usable by the owning org. Platform-provided types (`organization_id IS NULL`) are seeded by Aleph and cannot be created or modified by org members.
