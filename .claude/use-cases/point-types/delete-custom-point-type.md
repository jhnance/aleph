# Delete Custom Point Type

An org member deletes an org-defined custom point type. Same ownership enforcement pattern as update. Cannot delete a type that is still referenced by any point — FK constraint prevents it.
