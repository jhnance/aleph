# Publish Point Version

The SDK/CLI publishes a new version of a point. Assigns a semantic version string and an application-managed monotonic integer. Forward-propagates use cases from the previous version. Detects exports and connections. Locks the parent point row to serialize concurrent publishes.
