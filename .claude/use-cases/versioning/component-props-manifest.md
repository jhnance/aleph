# Component Props Manifest

A published version of a `frontend_component` point carries a props manifest — the set of component props active in that version, each with type, required flag, default value, and description. Props are stable identities (`component_props`); version-specific metadata lives in `point_version_component_props`. The manifest is immutable once a version is published.
