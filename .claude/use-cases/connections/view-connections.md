# View Connections

A user views the dependency graph for a point — outgoing connections (what this point depends on) and incoming connections (what depends on this point). Connections are version-specific and displayed in the context of a selected version.
