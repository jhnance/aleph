# Update Point Status

An org member transitions a point through its lifecycle: `active` → `deprecated` → `archived`. Archived points are not deleted — the FK chain makes hard deletion destructive. Status changes are reflected in the catalog UI and health checks.
