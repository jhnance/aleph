# Draft Use Case

An org member authors a draft use case for a point — either a brand-new use case (no lineage yet) or a new version of an existing one (lineage already exists). Drafts are freely editable. A `NULL` lineage_id means new; non-null means revision of an existing use case.
